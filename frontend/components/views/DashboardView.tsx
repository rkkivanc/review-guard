"use client";

export function DashboardView() {
  return (
    <div className="stub">
      <h2>Monitoring &amp; Decision Scoring</h2>
      <p className="muted">
        Next step: metric cards, score distribution, low-trust table, threshold slider, and the
        raw-vs-weighted average signature.
      </p>
    </div>
  );
}
